meta {
  name: OdsInstances
  type: http
  seq: 21
}

delete {
  url: {{API_URL}}/v3/odsInstances/{{CreatedOdsInstanceId}}
  body: none
  auth: inherit
}

script:post-response {
  test("DELETE OdsInstances: Status code is No Content", function () {
      expect(res.getStatus()).to.equal(204);
  });
  
}

settings {
  encodeUrl: true
}
