meta {
  name: DataStores Education Organizations
  type: http
  seq: 26
}

get {
  url: {{API_URL}}/v3/dataStores/edOrgs
  body: none
  auth: inherit
}

script:post-response {
  test("GET DataStores EdOrgs: Status code is OK", function () {
      expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
}
