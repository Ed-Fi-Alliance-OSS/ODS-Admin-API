meta {
  name: ResourceClaimClaimSets - Delete
  type: http
  seq: 38
}

delete {
  url: {{API_URL}}/v3/claimSets/{{CreatedClaimSetId}}/resourceClaimActions/4
  body: json
  auth: inherit
}

script:post-response {
  test("DELETE ResourceClaimOnClaimSet: Status code is No Content", function () {
      expect(res.getStatus()).to.equal(204);
  });
}

settings {
  encodeUrl: true
}
