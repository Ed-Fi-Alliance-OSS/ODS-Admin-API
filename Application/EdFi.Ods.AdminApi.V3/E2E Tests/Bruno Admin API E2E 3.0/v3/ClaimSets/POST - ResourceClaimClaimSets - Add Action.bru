meta {
  name: ResourceClaimClaimSets - Add Action
  type: http
  seq: 20
}

post {
  url: {{API_URL}}/v3/claimSets/{{CreatedClaimSetId}}/resourceClaimActions
  body: json
  auth: inherit
}

body:json {
  {
    "resourceClaimId": 4,
    "resourceClaimActions": [
      {
        "name": "read",
        "enabled": true
      },
      {
        "name": "create",
        "enabled": true
      }
    ]
  }
}

script:post-response {
  test("POST ResourceClaimAction: Status code is Created", function () {
      expect(res.getStatus()).to.equal(201);
  });
  
}

settings {
  encodeUrl: true
}
