meta {
  name: OdsInstanceContexts
  type: http
  seq: 22
}

delete {
  url: {{API_URL}}/v3/OdsInstanceContexts/{{CreatedOdsInstanceContextId}}
  body: none
  auth: inherit
}

script:post-response {
  test("DELETE OdsInstanceContexts: Status code is No Content", function () {
      expect(res.getStatus()).to.equal(204);
  });
  
}

settings {
  encodeUrl: true
}
