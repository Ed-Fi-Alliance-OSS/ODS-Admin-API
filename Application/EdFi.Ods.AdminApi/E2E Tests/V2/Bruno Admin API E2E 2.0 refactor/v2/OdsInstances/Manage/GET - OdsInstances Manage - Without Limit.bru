meta {
  name: OdsInstances Manage - Without Limit
  type: http
  seq: 10
}

get {
  url: {{API_URL}}/v2/odsinstances/manage?offset={{offset}}
  body: none
  auth: inherit
}

params:query {
  offset: {{offset}}
}

script:post-response {
  test("GET OdsInstances Manage Without Limit: Status code is OK", function () {
    expect(res.getStatus()).to.equal(200);
  });

  test("GET OdsInstances Manage Without Limit: Response is an array", function () {
    expect(res.getBody()).to.be.an("array");
  });
}

settings {
  encodeUrl: true
}
